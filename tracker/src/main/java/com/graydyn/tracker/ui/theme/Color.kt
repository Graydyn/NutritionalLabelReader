package com.graydyn.tracker.ui.theme

import androidx.compose.ui.graphics.Color

/** Accent colors used to distinguish macros across the tracker UI. */
val MacroCalories = Color(0xFFF57C00) // orange
val MacroProtein = Color(0xFFD32F2F)  // red
val MacroFat = Color(0xFFFBC02D)      // amber
val MacroCarbs = Color(0xFF388E3C)    // green
